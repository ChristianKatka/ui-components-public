import { HomeContainer } from './home/home.container';
import { NotFoundPageContainer } from './not-found-page/not-found-page.container';

export const components: any[] = [NotFoundPageContainer, HomeContainer];
